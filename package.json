{
  "name": "12-lead-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based 12-lead ECG simulator for paramedic education, behind an account gate.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node tests/session.test.mjs && node tests/gate.test.mjs && node tests/api.test.mjs",
    "standalone": "node build-standalone.js"
  },
  "engines": {
    "node": ">=18"
  }
}
