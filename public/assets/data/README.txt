# Derived PTB-XL profiles land here.
#
# Generate with:
#   python tools/ptbxl_profile.py "<path to PTB-XL 1.0.3>"
#
# The dataset itself (~2 GB) is never committed - only the small JSON of
# aggregate medians the script produces.
